/*package com.softwareverde.bitcoin.chain;

import com.softwareverde.bitcoin.type.database.DatabaseId;

public class BlockChainId extends DatabaseId {
    public static BlockChainId wrap(final Long value) {
        if (value == null) { return null; }

        return new BlockChainId(value);
    }

    protected BlockChainId(final Long value) {
        super(value);
    }
}
*/